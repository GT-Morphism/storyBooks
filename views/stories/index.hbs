<h1>Stories</h1>
<div class="row">
  {{#each stories}}
  <div class="col s12 m4">
    <div class="card">
      <div class="card-image">
        {{!-- user is the user relative to the loop; while ../user is the
        logged-in user (i. e. ../ let us jump outside of the loop). We still
        need to set this "../user"-variabel as a global variable, which is done
        inside app.js under the heading "Set Global Variable". Further, to parse
        HTML with handlebars, you have to use triple curly braces. --}}
        {{{editIcon user ../user _id}}}
      </div>
      <div class="card-content center-align">
        <h5>{{title}}</h5>
        <p>{{stripTags (truncate body 120)}}</p>
        <br />
        <div class="chip">
          <img src="{{user.image}}" referrerpolicy="no-referrer" />
          <a href="/stories/user/{{user._id}}">{{user.displayName}}</a>
        </div>
      </div>
      <div class="card-action center-align">
        <a href="/stories/{{_id}}" class="btn grey">Read More</a>
      </div>
    </div>
  </div>
  {{else}}
  <p>No Stories to display</p>
  {{/each}}
</div>
